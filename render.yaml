services:
  - type: web
    name: price-checker
    env: node
    # rootDir: server  # <-- убираем эту строку
    buildCommand: |
      cd app && npm install && npm run build
      cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
